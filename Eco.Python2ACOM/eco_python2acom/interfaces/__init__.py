"""ACOM interface definitions for EcoOS.

This package provides declarative interface definitions with inheritance.

Subpackages:
    guids: Component IDs (CID), Interface IDs (IID), and Generation IDs (GID).
    system: Core system interfaces (InterfaceBus, MemoryManager, FileSystem).

Modules:
    base: IEcoUnknown and IEcoComponentFactory base interfaces.
    decorators: @interface and @method decorators.
"""

__all__: list[str] = []
