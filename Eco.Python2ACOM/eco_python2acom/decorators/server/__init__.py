"""Server-side decorators: @view, @component, @factory.

Use these to implement ACOM components in Python — the C side will see them
as native components with proper vtable layout and identity per interface.
"""
