"""Decorators for declarative EcoOS definitions.

This package provides decorators for defining EcoOS structures, unions, interfaces, components,
views and factories in a declarative manner.

Decorators:
    @model: Define an EcoOS structure.
    @union: Define an EcoOS union.
    @interface: Define an EcoOS interface.
    @view: Define an implementation of interface inside a component.
    @component: Define an EcoOS component.
    @factory: Define an ACOM component factory.
"""
